# 贡献指南

> 感谢您对 ArtEcho 项目的关注！我们欢迎各种形式的贡献。

---

## 🤝 如何贡献

### 🐛 报告问题

1. **搜索现有问题**：在提交新问题前，请先搜索是否已有相关问题
2. **使用问题模板**：按照模板提供详细的问题信息
3. **包含复现步骤**：提供清晰的复现步骤和环境信息
4. **添加标签**：使用适当的标签分类问题

### 💡 功能建议

1. **描述清楚**：详细描述建议的功能和用途
2. **说明价值**：解释这个功能对用户的价值
3. **考虑实现**：如果可能，提供实现思路
4. **讨论优先级**：说明建议的紧急程度

### 📝 文档改进

1. **发现不足**：识别文档中的错误或缺失
2. **提供改进**：提交具体的改进建议
3. **保持一致性**：遵循现有的文档风格
4. **包含示例**：添加实际使用示例

---

## 🛠️ 开发流程

### 🌿 环境准备

1. **Fork 项目**
   ```bash
   git clone https://github.com/your-username/Hackathon_ArtEcho.git
   cd Hackathon_ArtEcho
   ```

2. **安装依赖**
   ```bash
   cd web
   npm install
   ```

3. **启动开发服务器**
   ```bash
   npm run dev
   ```

### 📋 开发规范

#### 代码风格

- **使用 TypeScript**：所有新代码使用 TypeScript
- **遵循 ESLint**：运行 `npm run lint` 检查代码质量
- **使用 Prettier**：运行 `npm run format` 格式化代码
- **组件化**：保持代码模块化和可复用

#### 命名规范

```javascript
// 变量和函数使用 camelCase
const userName = 'John';
const getUserInfo = () => {};

// 组件使用 PascalCase
const ImageGallery = () => {};

// 常量使用 UPPER_SNAKE_CASE
const API_BASE_URL = 'https://api.example.com';

// CSS 类名使用 kebab-case
.gallery-container {}
```

#### 注释规范

```javascript
/**
 * AI图像分析函数
 * @param {string} imageDataUrl - Base64编码的图像数据
 * @param {string} fileName - 图像文件名
 * @returns {Promise<AnalysisResult>} 分析结果
 */
async function analyzeWithWenxin(imageDataUrl, fileName) {
    // 实现细节
}
```

### 📤 提交流程

#### 1. 创建分支

```bash
# 创建功能分支
git checkout -b feature/your-feature-name

# 创建修复分支
git checkout -b fix/issue-number-description
```

#### 2. 提交更改

```bash
# 添加更改
git add .

# 提交（遵循约定式提交规范）
git commit -m "feat: add AI image analysis functionality"
```

#### 3. 推送分支

```bash
git push origin feature/your-feature-name
```

#### 4. 创建 Pull Request

- **使用模板**：填写 PR 模板
- **描述清楚**：详细说明更改内容和动机
- **添加截图**：如果是 UI 更改，添加前后对比
- **链接问题**：关联相关的 Issue

---

## 📝 提交信息规范

使用约定式提交格式：

```
<type>[optional scope]: <description>

[optional body]

[optional footer(s)]
```

### 类型说明

- `feat`: 新功能
- `fix`: 错误修复
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 代码重构
- `test`: 测试相关
- `chore`: 构建工具、依赖更新

### 示例

```bash
feat(ai): add wenxin model integration
fix: resolve image upload timeout issue
docs: update API documentation
style: fix linting errors in gallery component
```

---

## 🧪 测试规范

### 单元测试

```javascript
describe('AI Analysis', () => {
    test('should analyze image correctly', async () => {
        const result = await analyzeWithWenxin(testImageData);
        expect(result.title).toBeDefined();
        expect(result.confidence).toBeGreaterThan(80);
    });
});
```

### 集成测试

- **测试用户流程**：从上传到分析的完整流程
- **测试错误处理**：各种异常情况的处理
- **测试兼容性**：不同浏览器的兼容性

---

## 🎨 UI/UX 规范

### 设计原则

1. **用户友好**：界面简洁直观，操作流畅
2. **响应式设计**：适配各种屏幕尺寸
3. **无障碍性**：考虑色盲用户、键盘导航等
4. **性能优化**：快速加载，流畅交互

### 组件规范

- **保持一致性**：使用统一的设计语言
- **提供反馈**：加载状态、错误提示、成功消息
- **边界情况**：空状态、错误状态、加载状态

---

## 🔒 安全规范

### 数据安全

- **输入验证**：验证所有用户输入
- **XSS 防护**：避免跨站脚本攻击
- **数据加密**：敏感数据加密存储

### 隐私保护

- **最小权限**：只请求必要的权限
- **数据清理**：及时清理临时数据
- **用户控制**：用户可以删除自己的数据

---

## 📊 性能规范

### 加载性能

- **懒加载**：图片和组件按需加载
- **代码分割**：分割代码包，减少初始加载时间
- **缓存策略**：合理使用浏览器缓存

### 运行性能

- **避免内存泄漏**：及时清理事件监听器
- **优化渲染**：避免不必要的重绘和回流
- **批量操作**：合并 DOM 操作

---

## 🌍 国际化

### 添加新语言

1. **创建语言文件**
   ```javascript
   // locales/zh.json
   {
     "gallery.title": "画廊",
     "upload.placeholder": "拖拽图片到这里"
   }
   ```

2. **更新语言配置**
   ```javascript
   // i18n.config.js
   export const languages = {
     zh: '简体中文',
     en: 'English',
     // 添加新语言
   };
   ```

3. **翻译所有文本**
   - 用户界面文本
   - 错误消息
   - 帮助文档

---

## 📦 发布流程

### 版本规范

使用语义化版本：`MAJOR.MINOR.PATCH`

- **MAJOR**：不兼容的 API 更改
- **MINOR**：向后兼容的新功能
- **PATCH**：向后兼容的错误修复

### 发布检查清单

- [ ] 所有测试通过
- [ ] 文档已更新
- [ ] CHANGELOG 已更新
- [ ] 版本号已更新
- [ ] 安全检查通过
- [ ] 性能测试通过

---

## 🏆 贡献者认可

### 贡献类型

- **代码贡献**：功能开发、错误修复
- **文档贡献**：文档编写、翻译
- **设计贡献**：UI/UX 设计、图标
- **测试贡献**：测试用例、Bug 报告
- **社区贡献**：问题解答、社区推广

### 认可方式

- **贡献者列表**：在 README 中列出所有贡献者
- **发布感谢**：在发布说明中感谢贡献者
- **社区表彰**：在社区中突出优秀贡献

---

## 📞 联系方式

### 技术问题

- **GitHub Issues**：https://github.com/yanxuefengyan/Hackathon_ArtEcho/issues
- **讨论区**：https://github.com/yanxuefengyan/Hackathon_ArtEcho/discussions

### 非技术问题

- **邮箱**：contribute@artecho.com
- **社交媒体**：@ArtEcho_App

---

## 📋 检查清单

提交前请确认：

- [ ] 代码通过 ESLint 检查
- [ ] 代码通过 TypeScript 检查
- [ ] 添加了必要的测试
- [ ] 文档已更新
- [ ] 提交信息符合规范
- [ ] 没有 console.log 或调试代码
- [ ] 考虑了边界情况
- [ ] 测试了多种浏览器

---

## 🎉 感谢

感谢每一位为 ArtEcho 项目做出贡献的开发者！您的努力让这个项目变得更好。

> 通过共同努力，让艺术欣赏更加智能和普及。
